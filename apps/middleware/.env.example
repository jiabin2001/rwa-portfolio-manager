# No keys, RPC endpoints, LLM or model service required for the supported workflow.
EXECUTION_MODE=PAPER
API_PORT=3001
# Any other EXECUTION_MODE causes startup to fail. Never enter a real private key.
